/**
 * Write a for loop that loops through 1 -20
 * if the number is divisble by 3, print "Frontend"
 * if the number is divisble by 5, print "Simplified"
 * if the number is divisble by 3 and 5, print "Frontend Simplified"
 * If the Number is *not* divisible by either 3 or 5, print the number
 * 
 * Example:
 * 1 -> 1
 * 2 -> 2
 * 3 -> "Frontend"
 * 4 -> 4
 * 5 -> "Simplified"
 * ...
 * 15 -> "Frontend Simplified"
 * ...
 * 20 -> "Simplified"
 */

const str = `Welcome to Earth`

for (i = 0; i < str.length; i++){
    console.log(str[i]);
}